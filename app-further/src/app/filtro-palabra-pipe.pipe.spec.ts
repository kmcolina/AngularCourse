import { FiltroPalabraPipePipe } from './filtro-palabra-pipe.pipe';

describe('FiltroPalabraPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPalabraPipePipe();
    expect(pipe).toBeTruthy();
  });
});
